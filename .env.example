# Optional: keep the real document mapping outside the public repository.
BOI_DOCUMENTS_CONFIG=config/documents.json

# Optional: set a stable random value outside the repository.
BOI_SECRET_KEY=replace-with-a-long-random-secret
